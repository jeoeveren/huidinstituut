<!-- app/views/shared/_navbar.html.erb -->

<!-- Creates the navigation bar on top of the screen -->
<div class="navbar navbar-fixed-top">
  <div class="container">
    <div class="row">
      <div class="navbar-header">
        <a href="<%= root_path %>" class="navbar-brand">Huid & Lifestyle Support</a>
        <!-- Create a button with some lines in it that will be shown once the menu items can not be shown in the viewport -->
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navBarCollapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>

      <!-- Create a navigation bar -->
      <div class="collapse navbar-collapse" id="navBarCollapse">
        <!-- Menu items on the right side of the screen -->
        <ul class="nav navbar-nav navbar-right menu-0">
          <!-- Create a drop down with sun-menu for a specific menu item -->
          <li>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Onze merken<b class="caret"></b></a>
          <ul class="dropdown-menu menu-1">

            <!-- Add some rails stuff here -->
            <% @navbar_products.each do |product| %>
              <% if product.treatments.any? %>
                <li class="dropdown-submenu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= product.name %></a>
                <ul class="dropdown-menu menu-2">
                  <% product.treatments.each do |treatment| %>
                    <li><a href="#"><%= treatment.name %></a></li>
                  <% end %>
                </ul>
              <% else %>
                <li><a href="#"><%= product.name %></a></li>
              <% end %>

              </li>
            <% end %>
          </ul>
          </li>

          <!-- Create a drop down with sun-menu for a specific menu item -->
          <li>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Behandelingen<b class="caret"></b></a>
          <ul class="dropdown-menu menu-1">

            <!-- Add some rails stuff here -->
            <% @navbar_categories.each do |category| %>
              <li class="dropdown-submenu">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= category.name %></a>
              <ul class="dropdown-menu menu-2">
                <% category.treatments.all.order(:show_order, :name).each do |treatment| %>
                  <li><a href="#"><%= treatment.name %></a></li>
                <% end %>
              </ul>
              </li>
            <% end %>
          </ul>
          </li>

          <li><a href="#contact">Tarieven</a></li>

          <li><a href="#contact">Kalender</a></li>

          <li><a href="#contact">Afspraak</a></li>

          <!-- Create a drop down for a specific menu item -->
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Over ons<span class="caret"></span></a>
            <ul class="dropdown-menu menu-1">
              <li><a href="#">Het huidinstituut</a></li>
              <li><a href="#">Werken bij ons</a></li>
            </ul>
          </li>
          <li><a href="#contact">Contact</a></li>

          <!-- Create a drop down for a specific menu item -->
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Beheer<span class="caret"></span></a>
            <ul class="dropdown-menu menu-1">
              <li><%= link_to "Categorieën", categories_path %></li>
              <li><%= link_to "Labels", labels_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Products", products_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Behandelingen", treatments_path %></li>
            </ul>
          </li>

        </ul>
      </div> <!-- End collapse navigation bar -->
    </div> <!-- End row -->
  </div> <!-- End container -->
</div> <!-- End navigation bar -->
